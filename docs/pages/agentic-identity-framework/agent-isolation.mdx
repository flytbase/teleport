---
title: AI Agent Isolation
description: Provides architectural principles for isolating agentc workloads in order to manage agentic identity.
---

The Teleport Agentic Identity framework introduces a multi-layered approach to
securely running agentic workloads. In this approach, security is built into the
system. Meaning AI agents have a limited range of operations they are allowed to
complete, with each operation subject to auditing and access controls. 

The layers are:

- **Sandboxing:** Untrusted code is isolated and ephemeral, so there is minimal
  risk that it will interface with systems it is not supposed to.
- **Identity:** Each component of the system, including the end user, the
  agent's execution environment, and the agentic workload to execute, can
  prove itself to the system as a known, trusted subject.
- **Access controls:** Encoded in the identity is a list of operations and
  resources that the identity has access to, and the system checks access before
  allowing any operation to take place.
- **Audit:** Access controls associate each operation with an identity, allowing
  the system to construct logs of which identities performed which operations at
  different times.

## Sandboxing

- TODO: Why sandboxing is necessary
- TODO: hardware-level virtualization
- TODO: untrusted code and its execution enviornment exist ephemerally

## Identity

- TODO: Why identity is necessary
- TODO: Every component gets an identity: user certificates, certificate for
  tbot bot, SSH host cert for the beam, workload identity SVID, TLS user cert
  issued for the remote workload
- TODO: central auth service issues certs
- TODO: agents check certs against the CA
- TODO: the remote AI agent workload inherits the user's certificate and acts as
  the user.
- TODO: the remote AI agent uses its certificate to connect to resources. A
  mechanism is in place to allow the agent to transparently connect to resources
  without being aware of its environment.

## Access controls

- TODO: Why access controls are necessary
- TODO: Every identity gets roles encoded in certificate (general principle)
- TODO: user certificate allows the system to check RBAC for managing AI
  workloads
- TODO: Certificate for agent workload means that agents can check RBAC before
  passing traffic on to target infra resources
- TODO: Certificate for agent workload gets the same permissions as originating
  user because of a Teleport-trusted workload (tbot) that continuously updates
  certificates in the isolated environment

## Audit

- TODO: Why audit is necessary
- TODO: Identity for agent workloads means you get auditing for free
- TODO: Central auth service/proxy service/agent infrastructure allows for
  auditing, since traffic must flow through central locations, using
  certificates to authenticate identity, so we can reliably emit telemetry about
  specific user activity
- TODO: How identity delegation sessions work (how we can attribute AI agent
  activity to a user and workload identity)
- TODO: Attributing access to the originating user
- TODO: Attributing access to the AI agent's workload identity

## Reference implementation

TODO: explain how Beams implements the principles spelled out above
